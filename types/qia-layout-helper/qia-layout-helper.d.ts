/**
 *
 * This module serves as the JavaScript loader for the CSS style rules, therefore, no function call is necessary except an import of the corresponding .js file from JavaScript as ES module or HTML script (for the side effect).
 *
 * @module Qia.LayoutHelper
 */
declare class QiaLayoutHelper {
    constructor();
}
export default QiaLayoutHelper;
